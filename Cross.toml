
[target.aarch64-unknown-linux-gnu]
pre-build = [
    "apt-get update && apt-get install -y perl make cmake"
]

[target.x86_64-unknown-linux-gnu]
pre-build = [
    "apt-get update && apt-get install -y perl make cmake"
]

[target.x86_64-pc-windows-gnu]
pre-build = [
    "apt-get update && apt-get install -y perl make cmake"
]