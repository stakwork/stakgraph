{
  "name": "stakgraph-gateway-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Stakgraph gateway plugin admin UI (Preact + Vite + wouter + Tanstack Query + uPlot). Embedded into the Go plugin binary via //go:embed and served under /_plugin/ui/. See gateway/plans/phases/phase-8-observability-dashboard.md.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@preact/preset-vite": "^2.10.0",
    "@preact/signals": "^2.0.2",
    "@tanstack/react-query": "^5.62.0",
    "preact": "^10.25.0",
    "system-canvas": "^0.1.18",
    "system-canvas-react": "^0.1.24",
    "uplot": "^1.6.31",
    "wouter": "^3.4.2",
    "wouter-preact": "^3.4.2"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "^6.0.0"
  }
}
