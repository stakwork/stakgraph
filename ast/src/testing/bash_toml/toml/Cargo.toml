[package]
name = "demo"
version = "0.1.0"

[dependencies]
serde = "1.0"
tokio = { version = "1.43.0", features = ["rt-multi-thread"] }

[workflows]
ingest = { type = "function" }

[calls]
ingest = ["fetch", "parse"]
